datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

model credentials {
  id       Int     @id @default(autoincrement())
  email    String?
  password String?
}
